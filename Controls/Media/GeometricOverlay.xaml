<UserControl x:Class="ssi.GeometricOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="clr-namespace:WPFExtensions.Controls;assembly=WPFExtensions"

    Height="Auto" Width="Auto">

    <Grid Name="GeometricOverlayGrid"
        VerticalAlignment="Stretch"
        HorizontalAlignment="Stretch"
        Width="Auto"
        Height="Auto">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Column="0" Grid.Row="0" Name="overlayoGrid">
            <Canvas Name="canvas" Background="#00000000"></Canvas>
            <!-- Width="{Binding Path=ActualWidth, ElementName=overlayoGrid}" Height="{Binding Path=ActualHeight, ElementName=overlayoGrid}"-->
        </Grid>
    </Grid>
</UserControl>